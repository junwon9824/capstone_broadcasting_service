import { alpha, makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    cheifbtn: {
        height: '40px',
        marginLeft: '40%'
    },
    cheiftxt: {
        fontWeight: '600',
    },
}));