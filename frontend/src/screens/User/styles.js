import { alpha, makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    cheifbtn: {
        height: '40px',
    },
    cheiftxt: {
        fontWeight: '600',
    },
}));